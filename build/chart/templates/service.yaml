apiVersion: v1
kind: Service
metadata:
  name: {{ include "micrologger.fullname" . }}
  labels:
    {{- include "micrologger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "micrologger.selectorLabels" . | nindent 4 }}
